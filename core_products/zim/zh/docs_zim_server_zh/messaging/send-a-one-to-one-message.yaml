openapi: 3.0.0
info:
  title: open-api-desc
  version: 1.0.0
  contact:
    email: support@zegocloud.com

servers:
  - url: https://zim-api.zego.im
    description: ZIM 服务端 API 地址

tags:
  - name: Messaging
    description: 消息管理相关接口

paths:
  /:
    post:
      tags:
        - Messaging
      summary: SendPeerMessage
      description: |
        发送 1v1 单聊消息，支持向单个用户、或批量向多个用户发送消息。

        目标客户端将通过以下 ZIM SDK 的回调接口，接收单聊消息的通知。

        <Note title="说明">
        开通内容审核功能后，默认情况下服务端发送的消息会进行审核。如果服务端发送的消息不需要审核，请联系 ZEGO 技术支持进行配置。
        </Note>

        | iOS-25% | Android-25% | macOS-25% | Windows-25% |
        |-----|---------|-------|---------|
        | [peerMessageReceived](https://doc-zh.zego.im/article/api?doc=zim_API~objective-c_ios~protocol~ZIMEventHandler#zim-peer-message-received-info-from-user-id) | [onPeerMessageReceived](https://doc-zh.zego.im/article/api?doc=zim_API~java_android~class~ZIMEventHandler#on-peer-message-received) | [peerMessageReceived](https://doc-zh.zego.im/article/api?doc=zim_API~objective-c_macos~protocol~ZIMEventHandler#zim-peer-message-received-info-from-user-id) | [onPeerMessageReceived](https://doc-zh.zego.im/article/api?doc=zim_API~cpp_windows~class~ZIMEventHandler#on-peer-message-received) |

        | Web-25% | 小程序-25% | Flutter-25% | React Native-25% |
        |-----|---------|-------|---------|
        | [peerMessageReceived](https://doc-zh.zego.im/article/api?doc=zim_API~javascript_web~interface~ZIMEventHandler#peer-message-received) | [peerMessageReceived](https://doc-zh.zego.im/article/api?doc=zim_API~javascript_wxxcx~interface~ZIMEventHandler#peer-message-received) | [onPeerMessageReceived](https://pub.dev/documentation/zego_zim/latest/zego_zim/ZIMEventHandler/onPeerMessageReceived.html) | [peerMessageReceived](https://doc-zh.zego.im/article/api?doc=zim_API~javascript_react-native~interface~ZIMEventHandler#peer-message-received) |

        | Unity3D-25% | uni-app \| uni-app x-25% | HarmonyOS-25% |
        |-----|---------|-------|
        | [OnReceivePeerMessage](https://doc-zh.zego.im/article/api?doc=zim_API~cs_unity3d~class~ZIMEventHandler#on-receive-peer-message) | [peerMessageReceived](https://doc-zh.zego.im/article/api?doc=zim_API~javascript_uni-app~interface~ZIMEventHandler#peer-message-received) | [peerMessageReceived](https://doc-zh.zego.im/article/api?doc=zim_API~javascript_harmony~interface~ZIMEventHandler#peer-message-received) |

        <Note title="调用频率限制">20 次/秒。</Note>

        <Note title="说明">
        以下 `FromUserId` 和 `ToUserIds` 对应的用户已在客户端调用 `login` 方法登录 ZIM 服务，或开发者已调用 [服务端 API](./../user/batch-register-users.mdx) 注册相关的 userID。
        </Note>
      operationId: send-a-one-to-one-message
      parameters:
        - name: Action
          in: query
          required: true
          description: |
            > 接口原型参数
            >
            > https://zim-api.zego.im?Action=SendPeerMessage
          schema:
            type: string
            enum: ["SendPeerMessage"]
        - $ref: '../shared-components.yaml#/components/parameters/AppId'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureNonce'
        - $ref: '../shared-components.yaml#/components/parameters/Timestamp'
        - $ref: '../shared-components.yaml#/components/parameters/Signature'
        - $ref: '../shared-components.yaml#/components/parameters/SignatureVersion'
        
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SendOneToOneMessageRequest'
      responses:
        '200':
          description: 成功响应
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SendOneToOneMessageResponse'

components:
  schemas:
    SendOneToOneMessageRequest:
      type: object
      required:
        - FromUserId
        - ToUserIds
        - MessageType
        - MessageBody
      properties:
        FromUserId:
          type: string
          description: 发送者的用户 ID（已注册）。
          maxLength: 32
          example: "sender123"
        ToUserIds:
          type: array
          description: 接收者的用户 ID 列表，单次最多支持 100 个用户。
          maxItems: 100
          items:
            type: string
            maxLength: 32
            example: "receiver123"
        MessageType:
          type: integer
          description: |
            消息类型：
            - 1：文本消息
            - 2：图片消息
            - 3：文件消息
            - 4：音频消息
            - 5：视频消息
            - 11：自定义消息
            - 20：系统消息
          enum: [1, 2, 3, 4, 5, 11, 20]
          example: 1
        MessageBody:
          type: string
          description: 消息内容，详情请参考 [MessageBody 介绍](./messagebody-introduction.mdx)。
          maxLength: 32768
          example: "Hello, this is a test message"
        MessageSeq:
          type: integer
          format: int64
          description: 消息序号，用于消息去重。如果不传，系统会自动生成。
          example: 1234567890
        SendTime:
          type: integer
          format: int64
          description: 消息发送时间，Unix 时间戳，单位为毫秒。如果不传，系统会使用当前时间。
          example: 1640995200000

    SendOneToOneMessageResponse:
      allOf:
        - $ref: '../shared-components.yaml#/components/schemas/StandardResponse'
        - type: object
          properties:
            Result:
              type: array
              description: 发送成功结果
              items:
                type: object
                properties:
                  ToUserId:
                    type: string
                    description: 接收者用户 ID
                    example: "receiver123"
                  MessageSeq:
                    type: integer
                    format: int64
                    description: 消息序号
                    example: 1234567890
                  SendTime:
                    type: integer
                    format: int64
                    description: 消息发送时间，Unix 时间戳，单位为毫秒
                    example: 1640995200000
            ErrorList:
              type: array
              description: 发送失败列表
              items:
                type: object
                properties:
                  ToUserId:
                    type: string
                    description: 发送失败的接收者用户 ID
                    example: "receiver456"
                  SubCode:
                    type: integer
                    description: 发送失败的具体返回码
                    example: 660500002
                  SubMessage:
                    type: string
                    description: 发送失败的原因说明
                    example: "user not found"
